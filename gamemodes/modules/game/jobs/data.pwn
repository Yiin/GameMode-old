enum {
	JOB_RANK_LEADER = -1,
	JOB_RANK_ASSISTANT = -2
};

enum {
	JOB_NONE,
	JOB_TAXI,
	JOB_POLICE,
	JOB_MECH,
	JOB_FIRE,
	JOB_HEALTH,
	JOB_COUNT
};

new job_Name[JOB_COUNT][] = {
	"Bedarbis",
	"Taksi",
	"Policija",
	"Mechanikai",
	"Ugniagesiai",
	"Medikai"
};